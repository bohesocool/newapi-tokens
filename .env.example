# 复制本文件为 .env 并按需修改：cp .env.example .env
# .env 不会被提交到 git（见 .gitignore）

# NewAPI 的 PostgreSQL 容器名 / 用户 / 数据库名
PG_CONTAINER=postgres
PG_USER=root
PG_DB=new-api

# 要统计的 token 名称
TOKEN_NAME=ducker

# 首次启动时的初始登录密码（之后可在「设置」页修改；改后此项不再生效）
ADMIN_PASSWORD=admin
